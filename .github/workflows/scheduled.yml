name: scheduled

on:
  schedule:
    - cron: "6 */1 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scheduled:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: docs check
        run: |
          set -eu
          f="README.md"
          [ -f "$f" ] || exit 0
          bad=0
          for t in $(grep -oE "\(([^) ]+)\)" "$f" | tr -d "()" || true); do
            case "$t" in
              http*|"#"*) : ;;
              *) [ -e "$t" ] || bad=$((bad + 1)) ;;
            esac
          done
          echo "checked $f: $bad broken local link(s)"

      - name: update
        run: |
          set -eu
          f="README.md"
          [ -f "$f" ] || exit 0
          stamp="<!-- updated: $(date -u +%Y-%m-%d) -->"
          if grep -q "^<!-- updated: " "$f"; then
            sed -i "s|^<!-- updated: .*|$stamp|" "$f"
          else
            printf '\n%s\n' "$stamp" >> "$f"
          fi

      - name: commit
        run: |
          set -eu
          git config user.name "matthops20c9"
          git config user.email "279208084+matthops20c9@users.noreply.github.com"
          git add -- "README.md"
          git diff --cached --quiet && exit 0
          git commit -m "docs: update generated marker"
          git push
